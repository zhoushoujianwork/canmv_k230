# 汤圆AI门禁系统 - 屏幕交互功能设计

## 项目概述

基于K230+ESP32架构的智能门卫机器人，集成人脸识别、语音AI对话、射频控制等功能。本文档详细设计屏幕交互功能，重点加入AI对话状态显示和语音文本展示。

## 功能架构

### 核心状态机

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   IDLE      │────▶│ DETECTING   │────▶│ REGISTERING │
│ (欢迎界面)   │     │ (识别界面)   │     │ (注册界面)   │
└─────────────┘     └─────────────┘     └─────────────┘
       ▲                   ▲                   ▲
       │                   │                   │
       └───────────────────┼───────────────────┘
                           ▼
                   ┌─────────────┐
                   │  DIALOGUE   │
                   │ (对话界面)   │
                   └─────────────┘
```

## 界面设计规范

### 1. 屏幕布局架构

#### 全局布局结构
```
┌─────────────────────────────────────┐
│           状态栏 (Header)            │ ← 60px 高
│ ESP32状态 | 系统时间 | 网络状态        │
├─────────────────────────────────────┤
│                                     │
│           主内容区 (Main)           │ ← 自适应高度
│   - 欢迎信息/人脸检测/注册引导       │
│   - AI对话区域 (新增)               │
│                                     │
├─────────────────────────────────────┤
│        语音文本展示区 (Voice)        │ ← 80px 高 (新增)
│ 显示当前语音输入/输出的文本内容       │
├─────────────────────────────────────┤
│           底部控制栏 (Footer)        │ ← 60px 高
│ [注册] [对话] [设置] [历史] [清除]    │
└─────────────────────────────────────┘
```

#### 界面尺寸定义
- **LCD模式**: 800x480 (适用于K230 LCD屏幕)
- **HDMI模式**: 1920x1080 (适用于外部显示器)
- **响应式设计**: 支持动态布局调整

### 2. 配色方案

#### 主色调
- **主题色**: 青色 (0, 255, 255) - 科技感
- **成功色**: 绿色 (0, 255, 0) - 通过状态
- **警告色**: 黄色 (255, 255, 0) - 提示信息
- **错误色**: 红色 (255, 0, 0) - 错误状态
- **背景色**: 深灰色 (32, 32, 32) - 科技感

#### 状态指示色
```python
STATUS_COLORS = {
    "idle": (64, 64, 64),        # 灰色 - 待机
    "detecting": (0, 255, 0),    # 绿色 - 识别中
    "registering": (255, 165, 0), # 橙色 - 注册中
    "dialogue": (0, 255, 255),   # 青色 - 对话中
    "speaking": (255, 255, 0),   # 黄色 - 播放中
    "listening": (255, 0, 255),  # 紫色 - 聆听中
    "error": (255, 0, 0),        # 红色 - 错误
}
```

## AI对话功能设计

### 1. 对话状态管理

#### 对话状态枚举
```python
class DialogueState:
    INACTIVE = "inactive"     # 未激活
    LISTENING = "listening"   # 聆听中
    PROCESSING = "processing" # 处理中
    SPEAKING = "speaking"     # 播放中
    ERROR = "error"          # 错误状态
```

#### 状态转换规则
```
INACTIVE → LISTENING (用户触发/语音唤醒)
LISTENING → PROCESSING (检测到语音结束)
PROCESSING → SPEAKING (AI回复生成完成)
SPEAKING → INACTIVE (播放完成)
任意状态 → ERROR (出现错误)
ERROR → INACTIVE (错误处理完成)
```

### 2. 语音文本展示区设计

#### 区域布局
```
┌─────────────────────────────────────┐
│ 🎤 语音输入: [文本内容...]          │
│ 🔊 语音输出: [AI回复内容...]        │
│ 💬 状态提示: 正在聆听...           │
└─────────────────────────────────────┘
```

#### 文本滚动机制
- **单行显示**: 超出宽度自动滚动
- **多行支持**: 重要信息分行显示
- **动画效果**: 打字机效果显示AI回复

#### 显示优先级
1. **语音输入文本** - 最高优先级，实时显示
2. **AI回复文本** - 次高优先级，逐字显示
3. **状态提示信息** - 最低优先级，状态变化时显示

### 3. 对话界面设计

#### 界面布局
```
┌─────────────────────────────────────┐
│ 对话模式 | 00:00:00 | 信号: ●●●●  │ ← 状态栏
├─────────────────────────────────────┤
│                                    │
│          AI头像 & 动画             │ ← 中央区域
│                                    │
│   🤖 您好！我是汤圆AI助手         │
│   请问需要什么帮助吗？             │
├─────────────────────────────────────┤
│ 🎤 正在聆听您的语音...             │ ← 语音文本区
├─────────────────────────────────────┤
│ [🎤] [🔊] [设置] [❌]              │ ← 控制栏
└─────────────────────────────────────┘
```

#### 对话显示组件
- **对话气泡**: 区分用户和AI消息
- **头像显示**: AI助手头像
- **打字动画**: AI回复时的打字效果
- **状态指示器**: 语音输入/输出的可视化反馈

## 核心界面模块

### 1. 状态栏 (Header Bar)

#### 信息显示
- **ESP32状态**: IDLE/LISTENING/SPEAKING 等
- **系统时间**: 实时时钟显示
- **网络状态**: WiFi/连接状态指示
- **当前模式**: 识别/注册/对话 等

#### 状态栏代码示例
```python
def draw_header_bar(self, pl, title="汤圆AI门禁系统"):
    """绘制状态栏"""
    # 背景
    pl.osd_img.draw_rectangle(0, 0, self.display_size[0], 60,
                             color=(0, 0, 0), thickness=-1)
    pl.osd_img.draw_rectangle(0, 0, self.display_size[0], 60,
                             color=(0, 255, 255), thickness=2)

    # 标题
    pl.osd_img.draw_string_advanced(20, 20, 24, title,
                                   color=(255, 255, 255), scale=1)

    # ESP32状态 (右侧)
    esp32_text = f"ESP32: {self.esp32_state}"
    esp32_color = self.get_esp32_status_color()
    esp32_x = self.display_size[0] - 200
    pl.osd_img.draw_string_advanced(esp32_x, 20, 18, esp32_text, color=esp32_color)

    # 系统时间
    current_time = time.localtime()
    time_text = f"{current_time[3]:02d}:{current_time[4]:02d}:{current_time[5]:02d}"
    pl.osd_img.draw_string_advanced(esp32_x, 40, 16, time_text, color=(200, 200, 200))
```

### 2. 语音文本展示区 (Voice Text Area)

#### 核心功能
- **实时语音输入显示**: 显示用户说话内容
- **AI回复文本显示**: 逐字显示AI回复
- **状态提示**: 显示当前语音处理状态

#### 实现代码示例
```python
def draw_voice_text_area(self, pl):
    """绘制语音文本展示区"""
    area_y = self.display_size[1] - 140  # 距离底部140px
    area_height = 80

    # 背景框
    pl.osd_img.draw_rectangle(10, area_y, self.display_size[0]-20, area_height,
                             color=(0, 0, 0), thickness=-1)
    pl.osd_img.draw_rectangle(10, area_y, self.display_size[0]-20, area_height,
                             color=(0, 255, 255), thickness=1)

    # 语音输入行
    input_y = area_y + 10
    input_text = self.current_voice_input or "等待语音输入..."
    pl.osd_img.draw_string_advanced(20, input_y, 16, f"🎤 {input_text}",
                                   color=(255, 255, 255))

    # AI回复行
    reply_y = area_y + 35
    reply_text = self.current_ai_reply or "等待AI回复..."
    pl.osd_img.draw_string_advanced(20, reply_y, 16, f"🤖 {reply_text}",
                                   color=(0, 255, 255))

    # 状态提示行
    status_y = area_y + 55
    status_text = self.get_voice_status_text()
    status_color = self.get_voice_status_color()
    pl.osd_img.draw_string_advanced(20, status_y, 14, f"💬 {status_text}",
                                   color=status_color)
```

#### 语音状态管理
```python
def get_voice_status_text(self):
    """获取语音状态文本"""
    if self.dialogue_state == DialogueState.LISTENING:
        return "正在聆听..."
    elif self.dialogue_state == DialogueState.PROCESSING:
        return "正在处理..."
    elif self.dialogue_state == DialogueState.SPEAKING:
        return "正在播放..."
    elif self.dialogue_state == DialogueState.ERROR:
        return "语音处理错误"
    else:
        return "就绪"

def get_voice_status_color(self):
    """获取语音状态颜色"""
    status_colors = {
        DialogueState.LISTENING: (255, 165, 0),   # 橙色
        DialogueState.PROCESSING: (255, 255, 0),  # 黄色
        DialogueState.SPEAKING: (0, 255, 0),      # 绿色
        DialogueState.ERROR: (255, 0, 0),         # 红色
        DialogueState.INACTIVE: (128, 128, 128),  # 灰色
    }
    return status_colors.get(self.dialogue_state, (128, 128, 128))
```

### 3. 对话界面 (Dialogue Screen)

#### 界面元素
- **AI头像**: 中央显示，支持动画效果
- **对话气泡**: 显示对话历史
- **输入指示器**: 语音输入时的动画效果
- **控制按钮**: 对话相关控制

#### 对话界面代码示例
```python
def draw_dialogue_screen(self, pl):
    """绘制对话界面"""
    pl.osd_img.clear()

    # 状态栏
    self.draw_header_bar(pl, "AI对话模式")

    # 中央AI头像区域
    center_x = self.display_size[0] // 2
    center_y = self.display_size[1] // 2 - 40

    # AI头像背景圆圈
    avatar_radius = 60
    pl.osd_img.draw_circle(center_x, center_y, avatar_radius,
                          color=(0, 255, 255), thickness=3)

    # AI头像 (简单的emoji表示)
    pl.osd_img.draw_string_advanced(center_x - 20, center_y - 15, 32, "🤖",
                                   color=(0, 255, 255))

    # 打字动画效果
    if self.dialogue_state == DialogueState.SPEAKING:
        self.draw_typing_animation(pl, center_x, center_y + 80)

    # 欢迎文本
    welcome_text = "您好！我是汤圆AI助手"
    pl.osd_img.draw_string_advanced(center_x - 100, center_y + 120, 20,
                                   welcome_text, color=(255, 255, 255))

    # 语音文本展示区
    self.draw_voice_text_area(pl)

    # 底部控制栏
    self.draw_dialogue_toolbar(pl)
```

#### 打字动画效果
```python
def draw_typing_animation(self, pl, x, y):
    """绘制打字动画"""
    # 计算动画进度
    current_time = time.time()
    animation_progress = (current_time * 2) % 4  # 2秒一个循环

    # 三个点的动画
    dots = ""
    if animation_progress < 1:
        dots = "."
    elif animation_progress < 2:
        dots = ".."
    elif animation_progress < 3:
        dots = "..."
    else:
        dots = ""

    typing_text = f"AI正在回复{dots}"
    pl.osd_img.draw_string_advanced(x - 60, y, 18, typing_text,
                                   color=(0, 255, 255))
```

### 4. 底部控制栏 (Footer Toolbar)

#### 按钮布局
```
[注册] [对话] [设置] [历史] [清除]
  80px    80px   80px   80px   80px
```

#### 按钮状态管理
- **激活状态**: 高亮显示当前模式
- **禁用状态**: 灰色显示不可用功能
- **按下效果**: 按钮按下时的视觉反馈

#### 控制栏代码示例
```python
def draw_bottom_toolbar(self, pl):
    """绘制底部控制栏"""
    toolbar_y = self.display_size[1] - 60
    button_width = 80
    button_height = 50
    margin = 10

    buttons = [
        ("注册", "register", self.current_state == FaceRecognitionState.REGISTERING),
        ("对话", "dialogue", self.current_state == DialogueState.PROCESSING),
        ("设置", "settings", False),
        ("历史", "history", False),
        ("清除", "clear", False),
    ]

    for i, (text, action, is_active) in enumerate(buttons):
        x = margin + i * (button_width + margin)

        # 按钮背景
        bg_color = (0, 100, 100) if is_active else (64, 64, 64)
        border_color = (0, 255, 255) if is_active else (128, 128, 128)

        pl.osd_img.draw_rectangle(x, toolbar_y, button_width, button_height,
                                 color=bg_color, thickness=-1)
        pl.osd_img.draw_rectangle(x, toolbar_y, button_width, button_height,
                                 color=border_color, thickness=2)

        # 按钮文字
        text_color = (255, 255, 255) if is_active else (200, 200, 200)
        text_x = x + (button_width - len(text) * 12) // 2
        text_y = toolbar_y + 15
        pl.osd_img.draw_string_advanced(text_x, text_y, 18, text, color=text_color)
```

## AI对话集成设计

### 1. 状态同步机制

#### ESP32-K230通信协议
```
ESP32 → K230:
- DIALOGUE_START: 开始对话
- DIALOGUE_END: 结束对话
- VOICE_INPUT: "用户语音文本"
- AI_REPLY: "AI回复文本"
- VOICE_STATUS: "listening|processing|speaking"

K230 → ESP32:
- DIALOGUE_REQUEST: 请求开始对话
- DIALOGUE_CANCEL: 取消对话
```

#### 状态同步代码示例
```python
def handle_esp32_dialogue_sync(self, line):
    """处理ESP32对话状态同步"""
    try:
        parts = line.split("|")
        if len(parts) >= 2:
            command = parts[0]
            data = parts[1] if len(parts) > 1 else ""

            if command == "VOICE_INPUT":
                self.current_voice_input = data
                self.dialogue_state = DialogueState.PROCESSING
            elif command == "AI_REPLY":
                self.current_ai_reply = data
                self.dialogue_state = DialogueState.SPEAKING
                self.start_reply_animation()
            elif command == "VOICE_STATUS":
                self.update_voice_status(data)
    except Exception as e:
        print(f"处理对话同步失败: {e}")
```

### 2. 文本处理机制

#### 文本长度处理
- **最大显示长度**: LCD模式80字符，HDMI模式200字符
- **自动截断**: 超出长度显示"..."省略号
- **智能换行**: 长文本自动换行显示

#### 编码处理
- **UTF-8编码**: 支持中文显示
- **特殊字符处理**: 表情符号和特殊符号的显示
- **转义字符处理**: 处理换行符、制表符等

### 3. 动画效果设计

#### 语音输入动画
```python
def draw_voice_input_animation(self, pl, x, y):
    """语音输入时的动画效果"""
    if self.dialogue_state == DialogueState.LISTENING:
        # 波形动画
        wave_height = 20
        wave_count = 5
        base_y = y

        for i in range(wave_count):
            # 计算每个波的相位
            phase = (time.time() * 3 + i * 0.5) % (2 * math.pi)
            height = wave_height * (0.5 + 0.5 * math.sin(phase))

            wave_x = x + i * 15
            pl.osd_img.draw_line(wave_x, base_y - height, wave_x, base_y + height,
                               color=(0, 255, 255), thickness=3)
```

#### AI回复打字效果
```python
def draw_typing_text_effect(self, pl, text, x, y, start_time):
    """AI回复的打字效果"""
    elapsed = time.time() - start_time
    chars_to_show = int(elapsed * 20)  # 每秒20个字符

    display_text = text[:chars_to_show]
    if chars_to_show < len(text):
        display_text += "█"  # 光标效果

    pl.osd_img.draw_string_advanced(x, y, 16, display_text, color=(0, 255, 255))
```

## 性能优化

### 1. 绘制频率控制
- **状态栏**: 1秒更新一次
- **主内容区**: 50ms更新一次
- **语音文本区**: 实时更新
- **底部控制栏**: 状态变化时更新

### 2. 内存管理
- **对象池**: 重用界面元素对象
- **垃圾回收**: 及时清理临时对象
- **资源缓存**: 缓存常用图片和字体

### 3. 响应式设计
- **自适应布局**: 根据屏幕尺寸调整元素位置
- **字体缩放**: 根据分辨率调整字体大小
- **触摸区域**: 根据按钮大小调整触摸区域

## 扩展功能规划

### 1. 高级对话功能
- **多轮对话历史**: 显示最近5轮对话
- **语音情感识别**: 根据情感调整界面颜色
- **个性化设置**: 用户偏好设置

### 2. 无障碍功能
- **大字体模式**: 适合视力不佳的用户
- **高对比度模式**: 提高可读性
- **语音反馈**: 界面操作的语音提示

### 3. 统计信息
- **使用统计**: 每日对话次数、人脸识别次数
- **性能监控**: CPU使用率、内存占用
- **错误日志**: 系统异常记录

## 实现计划

### 第一阶段 (基础功能)
1. ✅ 设计界面布局架构
2. ✅ 定义配色方案和状态管理
3. ⏳ 实现状态栏和底部控制栏
4. ⏳ 添加语音文本展示区

### 第二阶段 (对话功能)
1. ⏳ 实现对话状态机
2. ⏳ 添加ESP32-K230通信协议
3. ⏳ 实现语音文本显示和动画

### 第三阶段 (优化完善)
1. ⏳ 添加动画效果和过渡
2. ⏳ 实现响应式布局
3. ⏳ 性能优化和内存管理

### 第四阶段 (高级功能)
1. ⏳ 多轮对话历史显示
2. ⏳ 无障碍功能支持
3. ⏳ 统计信息面板

---

**文档版本**: v1.0
**最后更新**: 2024年12月
**作者**: 汤圆AI团队